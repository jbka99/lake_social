# Lake Social Network
Простая социальная сеть на Flask.

## Как запустить:
1. Клонировать: `git clone ...`
2. Установить зависимости: `pip install -r requirements.txt`
3. Создать `.env` и вписать `DATABASE_URL` и `SECRET_KEY`.
4. Запустить: `python run.py`